---
import { ChevronRight } from '@lucide/astro'

export interface Props {
  title: string;
  image: string;
  description?: string;
  link: string;
}

 const { title, image, description, link } = Astro.props;
---

<article class="pb-8">
    <h2 class="font-bold text-2xl pb-6">{title}</h2>
    <p class="text-xs font-light">{description}</p>
    <img class="h-auto max-h-auto w-auto max-w-auto object-contain rounded-lg" src={image} alt={title} />
    <a href={link} target="_blank" rel="noopener noreferrer"> 
        <div class="flex items-center justify-center pt-4">
            <p class="underline inline-flex items-center">Cótiza ya<ChevronRight class="w-5 h-5 ml-1" /></p>
        </div>
    </a>
</article>

<style>

</style>